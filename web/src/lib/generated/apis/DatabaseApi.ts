/* tslint:disable */
/* eslint-disable */
/**
 * DevOpsBin API
 * Minimal OpenAPI starter spec for DevOpsBin runtime endpoints.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type AccountListResponse,
    AccountListResponseFromJSON,
    AccountListResponseToJSON,
} from '../models/AccountListResponse';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type TransferRequest,
    TransferRequestFromJSON,
    TransferRequestToJSON,
} from '../models/TransferRequest';
import {
    type TransferResult,
    TransferResultFromJSON,
    TransferResultToJSON,
} from '../models/TransferResult';

export interface PostTransferRequest {
    transferRequest: TransferRequest;
    xCSRFToken?: string;
    isolation?: PostTransferIsolationEnum;
    holdMs?: number;
}

/**
 * DatabaseApi - interface
 * 
 * @export
 * @interface DatabaseApiInterface
 */
export interface DatabaseApiInterface {
    /**
     * Creates request options for getAccounts without sending the request
     * @throws {RequiredError}
     * @memberof DatabaseApiInterface
     */
    getAccountsRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns every account across all users (id, owner username, name, and balance) so a transfer source and destination can be chosen. Requires a valid session; any authenticated user may call it. 
     * @summary List all accounts
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DatabaseApiInterface
     */
    getAccountsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountListResponse>>;

    /**
     * Returns every account across all users (id, owner username, name, and balance) so a transfer source and destination can be chosen. Requires a valid session; any authenticated user may call it. 
     * List all accounts
     */
    getAccounts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountListResponse>;

    /**
     * Creates request options for postTransfer without sending the request
     * @param {TransferRequest} transferRequest 
     * @param {string} [xCSRFToken] The CSRF token from the devopsbin_csrf cookie. Required in practice on state-changing requests to authenticated routes; a missing or mismatched token yields a 403 response. Must match the token bound to the current session. 
     * @param {'serializable' | 'repeatable-read' | 'read-committed'} [isolation] The transaction isolation level for the transfer. Defaults to &#x60;serializable&#x60; when omitted. 
     * @param {number} [holdMs] Optional delay, in milliseconds, applied inside the transaction after locking the accounts. Used to widen the contention window and demonstrate isolation and serialization retries under concurrency. 
     * @throws {RequiredError}
     * @memberof DatabaseApiInterface
     */
    postTransferRequestOpts(requestParameters: PostTransferRequest): Promise<runtime.RequestOpts>;

    /**
     * Moves the requested amount from the source account to the destination account inside a single transaction. The caller must own the source account. The transaction runs at the chosen isolation level (serializable by default) and retries automatically on serialization conflicts. Requires a valid session and a matching X-CSRF-Token header. 
     * @summary Transfer funds between two accounts
     * @param {TransferRequest} transferRequest 
     * @param {string} [xCSRFToken] The CSRF token from the devopsbin_csrf cookie. Required in practice on state-changing requests to authenticated routes; a missing or mismatched token yields a 403 response. Must match the token bound to the current session. 
     * @param {'serializable' | 'repeatable-read' | 'read-committed'} [isolation] The transaction isolation level for the transfer. Defaults to &#x60;serializable&#x60; when omitted. 
     * @param {number} [holdMs] Optional delay, in milliseconds, applied inside the transaction after locking the accounts. Used to widen the contention window and demonstrate isolation and serialization retries under concurrency. 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DatabaseApiInterface
     */
    postTransferRaw(requestParameters: PostTransferRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TransferResult>>;

    /**
     * Moves the requested amount from the source account to the destination account inside a single transaction. The caller must own the source account. The transaction runs at the chosen isolation level (serializable by default) and retries automatically on serialization conflicts. Requires a valid session and a matching X-CSRF-Token header. 
     * Transfer funds between two accounts
     */
    postTransfer(requestParameters: PostTransferRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TransferResult>;

}

/**
 * 
 */
export class DatabaseApi extends runtime.BaseAPI implements DatabaseApiInterface {

    /**
     * Creates request options for getAccounts without sending the request
     */
    async getAccountsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/accounts`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns every account across all users (id, owner username, name, and balance) so a transfer source and destination can be chosen. Requires a valid session; any authenticated user may call it. 
     * List all accounts
     */
    async getAccountsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountListResponse>> {
        const requestOptions = await this.getAccountsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountListResponseFromJSON(jsonValue));
    }

    /**
     * Returns every account across all users (id, owner username, name, and balance) so a transfer source and destination can be chosen. Requires a valid session; any authenticated user may call it. 
     * List all accounts
     */
    async getAccounts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountListResponse> {
        const response = await this.getAccountsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for postTransfer without sending the request
     */
    async postTransferRequestOpts(requestParameters: PostTransferRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['transferRequest'] == null) {
            throw new runtime.RequiredError(
                'transferRequest',
                'Required parameter "transferRequest" was null or undefined when calling postTransfer().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['isolation'] != null) {
            queryParameters['isolation'] = requestParameters['isolation'];
        }

        if (requestParameters['holdMs'] != null) {
            queryParameters['holdMs'] = requestParameters['holdMs'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xCSRFToken'] != null) {
            headerParameters['X-CSRF-Token'] = String(requestParameters['xCSRFToken']);
        }


        let urlPath = `/transfer`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TransferRequestToJSON(requestParameters['transferRequest']),
        };
    }

    /**
     * Moves the requested amount from the source account to the destination account inside a single transaction. The caller must own the source account. The transaction runs at the chosen isolation level (serializable by default) and retries automatically on serialization conflicts. Requires a valid session and a matching X-CSRF-Token header. 
     * Transfer funds between two accounts
     */
    async postTransferRaw(requestParameters: PostTransferRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TransferResult>> {
        const requestOptions = await this.postTransferRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TransferResultFromJSON(jsonValue));
    }

    /**
     * Moves the requested amount from the source account to the destination account inside a single transaction. The caller must own the source account. The transaction runs at the chosen isolation level (serializable by default) and retries automatically on serialization conflicts. Requires a valid session and a matching X-CSRF-Token header. 
     * Transfer funds between two accounts
     */
    async postTransfer(requestParameters: PostTransferRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TransferResult> {
        const response = await this.postTransferRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const PostTransferIsolationEnum = {
    serializable: 'serializable',
    repeatable_read: 'repeatable-read',
    read_committed: 'read-committed'
} as const;
export type PostTransferIsolationEnum = typeof PostTransferIsolationEnum[keyof typeof PostTransferIsolationEnum];
